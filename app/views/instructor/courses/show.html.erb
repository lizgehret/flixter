<script>
  $(function() {
    $('.lessons').sortable({
      // Triggering JQuery UI when a lesson is reordered and calling an item within ui
      update: function( event, ui ) {
        $.ajax({
          type: 'PATCH',
          url: ui.item.data('update-url'),
          dataType: 'json',
          data: { lesson: { row_order_position: ui.item.index() } }
        });
      }
    });
  });
</script>

<script>
  $(function() {
    $('.sections').sortable({
    // Triggering JQuery UI when a section is reordered and calling an item within ui
      update: function( event, ui ) {
        $.ajax({
          type: 'PATCH',
          url: ui.item.data('update-url'),
          dataType: 'json',
          data: { section: { row_order_position: ui.item.index() } }
        });
      }
    });
  });
</script>

<br />
<div class="booyah-box col-10 offset-1">
  <h1 class="text-center"><%= current_course.title %></h1>
  <br />
  <%= image_tag(current_course.image.url, class: 'img-fluid') if current_course.image? %>
  <br />
  <%= link_to 'Preview As A Student', course_path(current_course), class: 'btn btn-warning float-right' %>
  
  <br />
  <br />
  <div class="text-center">
    <%= link_to 'Add A New Section', new_instructor_course_section_path(current_course), class: 'btn btn-primary btn-lg ' %>
  </div>
  <br />

  <ul class="sections">
    <% current_course.sections.rank(:row_order).each do |section| %>
      <li data-update-url="<%= instructor_section_path(section) %>">
        <%= section.title %>
        <%= link_to 'New Lesson', new_instructor_section_lesson_path(section), class: 'float-right btn btn-primary btn-sm' %>
        <ul class="lessons">
          <% section.lessons.rank(:row_order).each do |lesson| %>
          <li data-update-url="<%= instructor_lesson_path(lesson) %>">
            <b><%= lesson.title %></b>
            -
            <%= lesson.subtitle %>
          </li>
          <% end %>
        </ul>
        <br class="clr" />
        <br />
      </li>
    <% end %>
  </ul>
</div>
